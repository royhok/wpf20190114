<Window x:Class="UpdateSourceTriggerTest.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:UpdateSourceTriggerTest"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" Title="MainWindow" Width="800" Height="450"
        mc:Ignorable="d">
    <Grid>
        <!--<TextBox Name="textBox1" Width="120" Height="23" Margin="230,41,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding ElementName=textBox2, Path=Text, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
        -->
        <!--  UpdateSourceTrigger=PropertyChanged  -->
        <!--
        <TextBox Name="textBox2" Width="120" Height="23" Margin="230,98,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />-->

        <TextBox Name="textBox1" Width="120" Height="23" Margin="230,41,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding ElementName=textBox2, Path=Text, Mode=TwoWay}" />
        <TextBox Name="textBox2" Width="120" Height="23" Margin="230,98,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding ElementName=textBox1, Path=Text, Mode=TwoWay}" />
    </Grid>

</Window>
